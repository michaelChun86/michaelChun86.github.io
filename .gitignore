# 에디터/로컬 설정 (배포 불필요)
.claude/

# 내부 기획 문서 (공개 리포에 올리지 않음)
CLAUDE_portfolio.md

# 미사용 배경음악 (사이트에서 참조하지 않음)
BGM/

# 원본 고해상도 이미지 라이브러리 전체 제외.
# 사이트가 실제 사용하는 샘플 이미지만 `git add -f` 로 강제 추가한다.
# (추후 WebP 최적화/분류 후 전체 업로드 예정)
Image/

# OS 부산물
Thumbs.db
.DS_Store
desktop.ini
